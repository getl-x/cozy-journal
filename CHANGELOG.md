# Cozy Journal 更新记录

## 1.6.1 — 2026-09-05

- 修复移动菜单打开后跨越桌面断点仍保留展开状态的问题。
- 为写作台自动保存增加超时中止与按钮恢复，并在登录状态过期时返回和显示明确提示。
- 自动保存草稿时不再生成普通文章修订，同时保留手动保存的历史修订行为。
- 为自定义主色生成独立深色和高对比度文字色，并同步自定义器实时预览。
- 为异常颜色导入值补充类型校验，避免数组数据触发 PHP 致命错误。

## 1.6.0 — 2026-08-29

- 将公开主题名称和后台默认品牌调整为中性的 Cozy Journal，并允许修改设置中心名称、说明和标记。
- 主题作者统一为 GetL-X，主题文件头、后台关于页和页尾来源统一链接到项目仓库。
- 页尾新增可关闭的主题来源行，可修改装饰符号和前缀文字，不加载外部图片。
- 主理人名称留空时使用当前登录用户的 WordPress `display_name`，已有站点保存值保持优先且不会被覆盖。
- 新安装默认隐藏文章作者、作者归档链接、评论头像、评论者个人网站链接、写作台用户名和头像，以及编辑锁中的协作者姓名。
- 设置中心扩展为 166 个设置，并继续与 WordPress 自定义器共用 theme mods。
- 新增完整私有备份与脱敏样式模板两种 JSON 导出；文件名和内容不记录本地导出时间。
- 保持写作台默认路径 `/write/`，升级不重置已有站点保存的自定义路径。
- 发布脚本保留正式主题预览图，排除本地预览目录和常见临时文件，固定 ZIP 条目时间，并让 `release-info.txt` 只保留版本与 SHA-256。
- 版本更新为 1.6.0，同时保留主题目录、Text Domain、代码前缀和备份主题标识等内部兼容项。
- 新增完全原创的 1200×900 正式主题预览图，由纸张、胶带、花朵、卡片和拍立得风景等本地绘制元素组成，不使用第三方图片素材。
- 保存按钮改用随文字尺寸缩放的内联 SVG 矢量对勾，避免 Dashicons 字形基线在不同分辨率和显示缩放下产生视觉偏移。
