# Cozy Journal 更新记录

## 1.3.0 — 2026-08-24

- 新增后台「前台写作 → 写作页面路径」设置。
- 写作路径支持一段英文小写、数字、短横线或下划线，默认仍为 `write`。
- 保存新路径后，网站页头、管理工具栏和后台欢迎页的写作入口会自动使用新地址。
- 新增动态请求识别，路径保存后可以立即访问，不依赖手动进入固定链接页面刷新规则。
- 路径修改后自动将旧重写规则标记为失效，旧写作路径不再继续打开写作台。
- 在 WordPress 自定义器的「前台写作」分区同步增加路径设置。
- 保留 v1.2.1 正式版作为独立回退版本，不覆盖任何历史发布。

## 1.2.1 — 2026-08-24

- 将前台手账写作功能确认为正式稳定版。
- 未登录访客访问 `/write/` 时不再显示登录表单，而是立即跳转到站点 `/404/`。
- 移除已经不再使用的前台登录表单代码和对应样式。
- 已登录用户的文章编辑、草稿自动保存、预览、发布、审核和媒体权限规则保持不变。
- 继续保留 v1.2.0 作为独立试用版和快速回退版本，不覆盖任何历史发布。

## 1.2.0 — 2026-08-24

- 新增虚拟前台写作地址 `/write/`，没有固定链接时自动支持查询参数入口。
- 新增温馨手账风前台写作台，支持标题、正文、摘要、分类、标签和评论设置。
- 接入 WordPress 可视化编辑器、文本模式和媒体库插入功能。
- 新增特色图片上传、替换和移除。
- 新增保存草稿、保存已发布文章修改、预览、发布和提交审核。
- 新增按 WordPress 角色能力执行的登录、编辑、发布和媒体上传权限检查。
- 新增草稿自动保存，可选择 30 秒、60 秒、2 分钟或关闭。
- 自动保存仅处理新文章、草稿和待审核文章，不自动修改已发布、私密或定时文章。
- 已发布、私密或定时文章的查看操作只打开已保存版本，避免预览时意外更新线上内容。
- 新增离开页面未保存提醒、字数统计和作者最近文章快捷入口。
- 新增网站页头、WordPress 管理工具栏和主题设置欢迎页的写作入口。
- 新增后台「前台写作」设置分区，并同步加入 WordPress 自定义器。
- 新增写作页禁止缓存、禁止搜索索引和虚拟地址规范化重定向保护。
- 新增中文逗号、顿号和分号标签分隔兼容。
- 将页头“写文章”按钮样式移入主样式表，确保普通前台页面正常显示。

## 1.1.0 — 2026-08-24

- 新增 WordPress 后台顶级菜单“手账主题设置”。
- 新增带左侧分区导航的独立主题设置中心。
- 新增欢迎、初始设置、全局样式、首页设置、文章与页面、手账装饰、备份恢复、关于主题八个页面。
- 新增顶部和底部保存按钮。
- 新增分区恢复默认值功能。
- 新增全部设置恢复默认值功能，仅清除本主题选项，不删除文章或媒体。
- 新增 JSON 设置导出与导入功能。
- 新增欢迎卡日期显示开关。
- 新增静态首页最新文章数量设置，可选择 3–12 篇。
- 保留并同步原有 WordPress 可视化自定义器设置。
- 新增专用后台样式和交互脚本，并适配窄屏后台界面。

## 1.0.0 — 2026-08-24

- 首次发布。
- 完成手账纸、纸胶带、贴纸、拍立得和文章拼贴视觉。
- 支持首页、文章、页面、归档、搜索、404、评论和小工具侧栏。
- 支持主题配色、字体、布局、装饰和页脚等可视化设置。
